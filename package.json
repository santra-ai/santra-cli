{
  "name": "santra-project",
  "version": "1.0.0",
  "description": "Santra monorepo for CLI, web, backend, and shared packages.",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "engines": {
    "bun": "1.3.11"
  },
  "license": "Apache-2.0",
  "packageManager": "bun@1.3.11",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "bunx tsc --noEmit --project tsconfig.json"
  },
  "workspaces": [
    "web",
    "cli",
    "packages/*"
  ]
}
