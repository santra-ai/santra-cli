{
  "name": "santra-project",
  "version": "1.0.0",
  "description": "Santra monorepo for CLI, web, backend, and shared packages.",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.11",
  "engines": {
    "bun": "1.3.11"
  },
  "license": "Apache-2.0",
  "scripts": {
    "backend": "bun --watch backend/src/index.ts",
    "cli": "bun cli/src/index.ts",
    "typecheck": "bunx tsc --noEmit --project tsconfig.json"
  },
  "workspaces": [
    "web",
    "cli",
    "core",
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
